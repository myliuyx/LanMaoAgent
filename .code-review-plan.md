# Agent Platform 代码审查计划

## 目标

对 `agent-platform` monorepo 进行系统性代码审查，覆盖 **7 个包 + CLI 应用**，确保：
- 核心业务逻辑正确性（Agent Loop、工具执行、LLM 交互）
- 安全性无漏洞（路径遍历、Shell 注入、权限控制）
- 类型安全与错误处理完整性
- 可维护性与代码质量

---

## 现状分析

### 项目架构概览

```
agent-platform/                    # pnpm monorepo (TypeScript)
├── packages/
│   ├── shared-types/              # 🔵 基础层 — 所有类型定义
│   ├── llm-adapter/               # 🟢 LLM 适配层 — Claude / OpenAI
│   ├── memory-stm/                # 🟡 短期记忆管理
│   ├── tool-core/                 # 🟠 工具注册 + Agent Loop 核心引擎
│   ├── tools/                     # 🔴 具体工具实现
│   │   ├── filesystem/            #    文件系统操作 (6个工具)
│   │   ├── git/                   #    Git 操作 (2个工具)
│   │   └── terminal/              #    Shell 命令执行 (1个工具 + 安全策略)
│   └── platform/                  # 🟣 平台编排层 — Session / Agent / 委托
├── apps/
│   └── cli/                       # ⚫ CLI 应用入口
└── tests/                         # 测试 (unit / contract / e2e)
```

### 模块依赖关系（有向图）

```
shared-types ← llm-adapter ← tool-core ← platform → apps/cli
                    ↑              ↑               ↑
                memory-stm       tools/*         platform
```

**关键发现：**
- `shared-types` 是纯类型包，无运行时依赖，被所有其他包引用
- `tool-core` 是核心引擎（Agent Loop + ToolRegistry），依赖 shared-types、llm-adapter、memory-stm
- `platform` 是最高层编排器，聚合了所有子模块
- `tools/*` 各自独立实现 `ToolHandler` 接口，通过 `tool-core/ToolRegistry` 注册

### 代码量估算

| 包 | 主要源文件数 | 预估行数 |
|---|---|---|
| shared-types | 6 TS 文件 | ~200 行（纯类型） |
| llm-adapter | 3 TS 文件 | ~500 行 |
| memory-stm | 2 TS 文件 | ~180 行 |
| tool-core | 3 TS 文件 | ~400 行 |
| tools/filesystem | 3 TS 文件 | ~450 行 |
| tools/git | 2 TS 文件 | ~120 行 |
| tools/terminal | 3 TS 文件 | ~280 行 |
| platform | 4 TS 文件 | ~350 行 |
| apps/cli | 1 TS 文件 + UI | ~350 行 |

---

## 实现步骤（分阶段审查）

### 第一阶段：基础类型层 — `shared-types`

**优先级：最高（所有其他模块的契约）**

#### 关注点：
1. **接口一致性** — `ChatMessage`, `ToolCall`, `ToolResult`, `Agent`, `ToolHandler` 等核心接口的字段是否完整、语义清晰
2. **类型安全性** — 是否有不必要的 `unknown` / `any`，联合类型是否穷尽覆盖
3. **默认值正确性** — `DEFAULT_CONFIG`, `DEFAULT_LLM`, `DEFAULT_AGENT_PARAMS` 的默认值是否符合预期行为
4. **导出完整性** — `index.ts` 是否正确导出了所有需要对外暴露的类型和常量

#### 具体检查项：
- [ ] `ToolResult.retryable` 字段是否在所有消费端被正确使用？
- [ ] `ChatMessage.role` 联合类型 `'system' | 'user' | 'assistant' | 'tool'` 是否完整？
- [ ] `Agent.tools: ToolHandler[]` — 空数组 vs undefined 的语义差异
- [ ] `ToolExecutionContext.depth?` 可选字段的使用场景

---

### 第二阶段：LLM 适配层 — `llm-adapter`

**优先级：高（外部 API 交互，直接影响功能正确性）**

#### 关注点：
1. **ClaudeAdapter** (`ClaudeAdapter.ts`)
   - [ ] Anthropic Messages API v1 的请求格式是否正确？特别是 `system` 消息处理和 tool_use block 的序列化
   - [ ] 响应解析是否处理了所有可能的 content block type（text, tool_use）？
   - [ ] finish_reason 映射：`end_turn→stop`, `tool_use→tool_calls`, `max_tokens→length` 是否正确？
   - [ ] token usage 字段名映射：`input_tokens/output_tokens` vs `promptTokens/completionTokens`

2. **OpenAIAdapter** (`OpenAIAdapter.ts`) — ⚠️ **最复杂模块，重点审查**
   - [ ] SSE 流式解析的边界条件：buffer 累积、行分割、不完整 JSON 的处理
   - [ ] `isBalancedJson()` 函数是否能正确处理嵌套对象中的引号/转义？
   - [ ] `safeJsonParse()` + balanced check 的双重回退策略是否合理？
   - [ ] tool_calls 增量解析：`argBuffers` Map 管理是否正确（idx 作为 key）？
   - [ ] 非流式模式下的响应解析是否与流式模式保持一致？

3. **通用问题**
   - [ ] 重试逻辑：指数退避 `2^attempt`，最大延迟 5000ms，最多 2 次重试 — 是否合理？
   - [ ] AbortError 检测：`e instanceof DOMException && e.name === 'AbortError'` — 跨环境兼容性？
   - [ ] timeoutSec 超时控制：`AbortController` + `setTimeout` 的清理（finally/clearTimeout）
   - [ ] API Key 安全性：是否在任何地方打印或暴露了密钥？

---

### 第三阶段：短期记忆层 — `memory-stm`

**优先级：中（影响上下文窗口管理和 token 预算）**

#### 关注点：
1. **ShortTermMemory.compact()** — ⚠️ **核心压缩逻辑**
   - [ ] `MAX_COMPACT_COUNT = 5` 后进入 drop mode，返回固定 `n * 10` — 这个估算值是否合理？
   - [ ] compact 后的消息重建：`messages.splice(-keepCount)` + `stmContext` 拼接是否正确？
   - [ ] `cumulativeTokens` 重置逻辑：在 `runAgentLoop` 中调用后，新 memory 实例的初始计数为 0

2. **add() 方法**
   - [ ] `structuredClone(message)` — Node.js 兼容性（需要 Node ≥ 17）？
   - [ ] maxCapacity 为 -1 时的行为：条件 `maxCapacity > 0` 是否正确处理了无限制场景？

3. **extractToolSummary()**
   - [ ] JSON 解析截断风险：只取前 500 字符做 JSON.parse，可能遇到不完整 JSON — try/catch 已覆盖

---

### 第四阶段：工具核心层 — `tool-core`（⭐ 最关键的模块）

**优先级：最高（Agent Loop + ToolRegistry = 整个系统的引擎）**

#### 4a. ToolRegistry.ts — ⚠️ **安全关键**

1. **路径安全检查 `checkAllowed()`**
   - [ ] `extractPathValues()` 递归深度限制为 20，是否能覆盖所有嵌套场景？
   - [ ] `realpathSync` 解析：对不存在的文件 fallback 到 `resolve(p)` — 是否可能被利用？
   - [ ] 路径前缀匹配：`resolved.startsWith(realAllowed + '/')` — 末尾 `/` 的添加是否正确防止了前缀攻击（如 `/workspace` vs `/workspace-evil`）？

2. **错误处理**
   - [ ] `shouldRetry()` 的正则 `/enoent|eacces|eperm|eisdir|enotdir|eexist/` — 是否遗漏了其他不应重试的错误码？
   - [ ] `safeErrorMessage()` 对非 Error 对象的提取逻辑

3. **execute() 流程**
   - [ ] 先检查路径安全，再查找 handler — 顺序正确（安全优先）
   - [ ] 未知 tool name 返回 `retryable: false` — 合理

#### 4b. runAgentLoop.ts — ⚠️ **核心业务逻辑**

1. **Token 预算与压缩触发**
   - [ ] `estimateTokens()` 使用 `TextEncoder.encode(text).length / 3.5` — 这个估算精度如何？
   - [ ] 双重触发条件：`cumulativeTokens > budget || llmTokenEstimate > budget * 3` — 为什么是 3 倍？
   - [ ] `consecutiveCompactFailures >= 3` 后返回失败 — 是否过于激进？

2. **Agent Loop 主循环**
   - [ ] `depth` 限制：默认 maxDepth=5，防止无限递归委托
   - [ ] tool_calls 执行后的消息追加顺序是否正确（assistant → tool results）？
   - [ ] STM sync：使用 `newMsgStart` 索引增量同步 — 在 compact 后重建 messages 时是否保持一致性？

3. **重试机制**
   - [ ] 指数退避延迟：`Math.min(1000 * Math.pow(2, attempt - 1), 4000)` — 1s, 2s, 4s 上限
   - [ ] AbortSignal 检查在等待期间是否正确中断？

---

### 第五阶段：工具实现层 — `tools/*`

**优先级：高（直接操作文件系统/Shell，安全风险最高）**

#### 5a. FilesystemHandler.ts — ⚠️ **安全关键**

1. **路径验证**
   - [ ] `validatePath()` 对写操作的祖先目录检查：`validateAncestors()` 的递归逻辑
   - [ ] `safeResolve()` + `realpathSync` 在文件不存在时的 fallback 行为
   - [ ] `isInAllowedPath()` 的前缀匹配是否被 `+ '/'` 正确保护？

2. **各工具方法**
   - [ ] `grep()`: regex 编译失败处理，结果数量限制 `GREP_MAX_RESULTS=200`
   - [ ] `grepR()`: 递归深度限制 `MAX_DEPTH=50`，截断标记 `[truncated]`
   - [ ] `writeFile()`: append 模式参数默认值处理（`undefined` vs `false`）
   - [ ] `editFile()`: 
     - regex 模式下无匹配时的错误返回
     - 非 regex 多匹配时只替换第一个的逻辑是否正确？
     - `split(oldString).join(newString)` 在 oldString 为空字符串时的行为（会产生空数组！）

3. **参数验证**
   - [ ] `MAX_STRING_ARG_LENGTH = 64KB` — 是否足够？write_file 的 content 参数限制
   - [ ] 所有工具都有 path 参数的类型和存在性校验吗？

#### 5b. GitHandler.ts

1. **execGit()**
   - [ ] `cwd` 必须为绝对路径的检查（`isAbsolute(ctx.cwd)`）— 但 Platform 传入的 cwd 已经是 `process.cwd()`，是否足够安全？
   - [ ] timeout=10s — 对于大型仓库的 diff/status 操作是否足够？

2. **工具设计**
   - [ ] `git_diff` 和 `git_status` 无参数 — agent 无法指定分支/范围，功能受限但降低了安全风险

#### 5c. TerminalHandler.ts + shellSecurity.ts — ⚠️ **安全关键**

1. **Shell 注入防护 (`shellSecurity.ts`)**
   - [ ] `DANGEROUS_METACHARACTERS = /[;`]|\$\(|\${.*}/` — 是否遗漏了其他危险字符？如 `&&`, `||`, `|`, `>`, `<`, `&`
   - [ ] `ALWAYS_DANGEROUS_PATTERNS`: 
     - `rm -rf/-fr` 检测 — 但 `rm -r`（无 f）是否允许？
     - `chmod 0xxx/777/666/444/222/111` — 列表是否完整？
     - `dd/mkfs/fdisk` — 磁盘操作拦截

2. **命令白名单**
   - [ ] `DEFAULT_WHITELIST` 包含 30+ 个命令 — 是否有不安全的命令混入？（如 `curl`, `wget`, `python`, `node -e`）
   - [ ] `ALLOWED_COMMAND_PATTERNS` 精确匹配：`'pnpm install'`, `'tsc --build'` 等 — agent 能否使用带额外参数的变体？

3. **命令解析器 (`parseCommand()`)**
   - [ ] 引号处理逻辑是否能正确处理嵌套引号和转义？
   - [ ] 对空命令或纯空格输入的处理

4. **execFile 安全**
   - [ ] `maxBuffer: 20KB` — 输出截断保护
   - [ ] timeout=30s — 防止无限挂起
   - [ ] 错误信息提取的健壮性（处理非 Error 对象）

---

### 第六阶段：平台编排层 — `platform`

**优先级：高（聚合所有模块，负责 Session/Agent 生命周期）**

#### 关注点：
1. **Platform.ts 构造函数**
   - [ ] LLM provider 选择逻辑：anthropic 必须提供 apiKey，openai 可选
   - [ ] ToolHandler 注册顺序是否影响工具发现？
   - [ ] `DelegateToAgentHandler` 的构造参数传递是否正确（compressionRatio, maxToolRetries）？

2. **Session 管理**
   - [ ] Session key 格式 `${agentId}::${projectRoot}` — 是否有冲突风险？
   - [ ] Session 复用：跨 `run()` 调用保留消息历史，但 aborted 状态不持久化
   - [ ] STM 的 `resetCompactCount()` 在 createSession 时调用

3. **Platform.run()**
   - [ ] AbortSignal 检查在入口处和 runAgentLoop 内部的双重检查
   - [ ] 错误处理：catch 块中对各种异常类型的统一包装
   - [ ] Session messages 的持久化：`session.messages = messages` — 类型断言 `(session as typeof session & { messages })` 是否必要？

4. **agents.ts**
   - [ ] `BUILTIN_AGENTS` 中 orchestrator 必须存在，且 systemPrompt 包含子 agent 列表
   - [ ] `buildAgent()` 的参数合并逻辑：`DEFAULT_AGENT_PARAMS` → runtime params

---

### 第七阶段：CLI 应用层 — `apps/cli`

**优先级：中（用户交互入口）**

#### 关注点：
1. **配置加载 (`loadConfig`, `loadAgents`)**
   - [ ] 配置文件路径：`~/.agent-platform/config.json` — 跨平台兼容性（os.homedir）
   - [ ] Agent extends 循环检测：`visited: Set<string>` 是否正确实现？
   - [ ] 默认 agent 回退机制：文件不存在时 fallback 到 BUILTIN_AGENTS

2. **CLI 交互**
   - [ ] SIGINT 处理：abortController.abort() + pendingQueue 清空
   - [ ] ESC 取消排队：仅 TTY 环境，raw mode 的启用/恢复
   - [ ] 输入队列机制：running=true 时新输入进入 pendingQueue

3. **依赖注入**
   - [ ] `runCli(deps)` 的可测试性设计 — 所有外部依赖都通过 deps 传入

---

### 第八阶段：测试覆盖审查

#### 关注点：
1. **单元测试 (`tests/unit/`)** — 按包组织，与源码对应
2. **契约测试 (`tests/contract-tests/llm.spec.ts`)** — LLM API 交互的接口一致性
3. **E2E 测试 (`tests/e2e/encoding-flow.spec.ts`)** — 端到端流程验证

#### 检查项：
- [ ] 每个核心模块是否有对应的单元测试？
- [ ] 安全相关逻辑（路径检查、shell 注入检测）是否有专门的测试用例？
- [ ] LLM adapter 的流式解析边界条件是否有测试覆盖？
- [ ] Agent Loop 的 compact/重试/深度限制等边界场景是否有测试？

---

## 涉及文件列表

### Phase 1 — shared-types（6 个文件）
| 文件 | 说明 |
|---|---|
| `packages/shared-types/src/index.ts` | 统一导出入口 |
| `packages/shared-types/src/chat.ts` | ChatMessage, ToolCall, ToolResult, ToolDefinition |
| `packages/shared-types/src/agent.ts` | Agent, ToolHandler, ToolExecutionContext |
| `packages/shared-types/src/config.ts` | 所有配置类型 + 默认值常量 |
| `packages/shared-types/src/llm.ts` | ChatResponse, TokenUsage, AgentResult |
| `packages/shared-types/src/logger.ts` | Logger 接口 + consoleLogger |

### Phase 2 — llm-adapter（3 个文件）
| 文件 | 说明 |
|---|---|
| `packages/llm-adapter/src/ClaudeAdapter.ts` | Claude API 适配器 |
| `packages/llm-adapter/src/OpenAIAdapter.ts` | OpenAI API 适配器（含 SSE 流式解析） |
| `packages/llm-adapter/src/index.ts` | 统一导出 + LLMAdapter 接口 |

### Phase 3 — memory-stm（2 个文件）
| 文件 | 说明 |
|---|---|
| `packages/memory-stm/src/ShortTermMemory.ts` | STM 核心逻辑 + compact |
| `packages/memory-stm/src/index.ts` | 导出入口 |

### Phase 4 — tool-core（3 个文件）⭐
| 文件 | 说明 |
|---|---|
| `packages/tool-core/src/ToolRegistry.ts` | 工具注册 + 路径安全检查 ⚠️ |
| `packages/tool-core/src/runAgentLoop.ts` | Agent Loop 核心引擎 ⭐ |
| `packages/tool-core/src/index.ts` | 统一导出 |

### Phase 5 — tools/*（8 个文件）⚠️
| 文件 | 说明 |
|---|---|
| `packages/tools/filesystem/src/FilesystemHandler.ts` | 6 个文件系统工具 ⚠️ |
| `packages/tools/filesystem/src/pathUtils.ts` | 路径安全工具函数 ⚠️ |
| `packages/tools/git/src/GitHandler.ts` | Git diff/status 工具 |
| `packages/tools/terminal/src/TerminalHandler.ts` | Shell 命令执行 ⚠️ |
| `packages/tools/terminal/src/shellSecurity.ts` | Shell 注入防护 ⚠️ |

### Phase 6 — platform（4 个文件）
| 文件 | 说明 |
|---|---|
| `packages/platform/src/Platform.ts` | 平台主类 + Session 管理 |
| `packages/platform/src/agents.ts` | BUILTIN_AGENTS + buildAgent |
| `packages/platform/src/tools/delegateTool.ts` | 子 Agent 委托工具 |
| `packages/platform/src/index.ts` | 统一导出 |

### Phase 7 — apps/cli（1+ 个文件）
| 文件 | 说明 |
|---|---|
| `apps/cli/src/index.ts` | CLI 入口 + 配置加载 + 交互循环 |

### 测试文件
| 目录/文件 | 说明 |
|---|---|
| `tests/unit/*` | 各模块单元测试 |
| `tests/contract-tests/llm.spec.ts` | LLM API 契约测试 |
| `tests/e2e/encoding-flow.spec.ts` | 端到端编码流程测试 |

---

## 注意事项 / 风险

### 🔴 高风险（必须优先审查）

1. **路径遍历攻击** — `ToolRegistry.checkAllowed()` + `pathUtils.ts` 中的路径验证逻辑是整个系统的安全基石。如果存在绕过，agent 可以读写任意文件。
   - 重点检查：symlink 解析、前缀匹配边界条件、write 操作的祖先目录验证

2. **Shell 注入** — `shellSecurity.ts` 的正则和模式匹配是否足够全面？遗漏的危险字符（如 `&&`, `||`, `|`, `>`, `<`, `&`）可能导致任意命令执行。
   - 重点检查：白名单中是否有不安全的命令（curl, wget, python, node -e 等）

3. **OpenAI SSE 解析** — `isBalancedJson()` 和增量 JSON 解析是 OpenAIAdapter 中最复杂的部分，任何 bug 都可能导致工具调用参数解析错误。
   - 重点检查：嵌套对象中的引号/转义处理、不完整 JSON chunk 的累积

### 🟡 中风险

4. **Agent Loop 无限循环** — `maxIterations` + `depth` 双重限制，但 compact 失败后的行为可能导致意外退出或资源耗尽。
5. **Token 估算精度** — `estimateTokens()` 使用简单的字节/3.5 估算，可能与实际 LLM tokenization 有较大偏差，导致预算触发不准确。
6. **Session 消息持久化** — Platform.run() 中的类型断言 `(session as typeof session & { messages })` 暗示了类型系统的不一致性。

### 🟢 低风险 / 改进建议

7. **代码重复** — `safeErrorMessage()` 在 ToolRegistry、GitHandler、TerminalHandler 中都有实现，应提取为共享工具函数。
8. **错误处理不一致** — 有些模块 throw error，有些返回 `{ isError: true }`，调用方需要同时处理两种情况。
9. **硬编码值** — `DEFAULT_WHITELIST`, `ALLOWED_COMMAND_PATTERNS`, `GREP_MAX_RESULTS` 等常量分散在各处，建议集中管理。

### 📋 审查顺序总结

```
Phase 1: shared-types     → 理解契约（200行）
    ↓
Phase 4a: ToolRegistry    → 安全基石（⭐最高优先级）
    ↓
Phase 5a: FilesystemHandler + pathUtils → 路径安全实现 ⚠️
    ↓
Phase 5c: TerminalHandler + shellSecurity → Shell 安全 ⚠️
    ↓
Phase 2: llm-adapter      → LLM 交互（OpenAI SSE 为重点）
    ↓
Phase 4b: runAgentLoop    → Agent Loop 核心引擎 ⭐
    ↓
Phase 3: ShortTermMemory  → 记忆管理
    ↓
Phase 6: Platform         → 编排层整合
    ↓
Phase 7: apps/cli         → CLI 入口
    ↓
Phase 8: tests            → 测试覆盖评估
```

**总审查量：约 2,500 行源代码 + 测试文件，分 8 个阶段逐步完成。**
