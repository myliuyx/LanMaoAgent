# Agent Platform — 项目全局结构分析

## 一、目录结构

```
agent-platform/
├── apps/cli/src/index.ts           # CLI 入口 (runCli/loadConfig/loadAgents)
│   └── ui/{renderer,spinner,theme,tool-view}
├── packages/
│   ├── shared-types/               # ① 共享类型（零依赖）
│   │   src: {index,chat,llm,agent,config,logger}.ts
│   ├── memory-stm/                 # ② STM (→shared-types)
│   ├── llm-adapter/                # ③ LLM适配 (→shared-types)
│   ├── tool-core/                  # ④ 核心引擎 (→shared-types+memory-stm+llm-adapter)
│   ├── platform/                   # ⑤ 编排层 (依赖所有上游包)
│   └── tools/{filesystem,git,terminal}  # ⑥ 工具实现
├── tests/unit/contract-tests/e2e/
├── docs/ coverage/
├── package.json pnpm-workspace.yaml tsconfig*.json
├── vitest.config.ts eslint.config.js build.mjs AGENTS.md
```

## 二、技术栈

pnpm 9.0 + TS 6.0 (strict, ESM) + tsc --build + esbuild 0.28 + Vitest 4.1 (v8 coverage ≥80%) + ESLint 10 flat config + Prettier 3.8

## 三、包依赖图（实际代码验证）

```
shared-types (零依赖)
  ├── memory-stm / llm-adapter / tools/* (→shared-types)
  ├── tool-core (→shared-types+memory-stm+llm-adapter)
  └── platform (→所有上游包) → apps/cli
```

## 四、核心模块

| 模块 | 职责 | 关键实现 |
|------|------|----------|
| shared-types | 类型契约层 | ChatMessage/ToolCall/AgentResult等接口+DEFAULT常量 |
| ShortTermMemory | 短期记忆管理 | Ring Buffer + compact()角色分组摘要, structuredClone防变 |
| ClaudeAdapter | Anthropic API适配 | system提取/tool_use映射/重试(429/502/503) |
| OpenAIAdapter | OpenAI API适配 | SSE流式解析/function_call映射 |
| ToolRegistry | 工具注册执行 | 双映射(handlers by id, toolToHandler by name)+全局路径安全防线 |
| runAgentLoop | Agent主循环 | LLM迭代→工具调用→STM compact(预算=contextWindow×compressionRatio) |
| Platform | 编排层 | 组装组件/createSession()/run() |
| BUILTIN_AGENTS | 5个预定义Agent | orchestrator/coding/planner/reviewer/tester |

## 五、安全设计

- ToolRegistry.execute(): path.resolve + realpathSync + startsWith 全局防线
- FilesystemHandler: 路径白名单 + symlink解析 + ancestor校验(写操作)
- TerminalHandler: 命令白名单(30+) + shell metacharacter注入检测

## 六、关键实现细节

1. Agent Loop: system prompt放入messages数组, ClaudeAdapter内部提取为Anthropic system参数
2. Session: 无SessionManager, createSession()直接返回对象; 每次run()创建新session
3. Agent查找: agentsCache.get(id), 非Registry模式
4. 错误处理: 所有工具execute返回{content, isError}, 不抛异常
5. 重试: 指数退避(1s/2s/4s), 最多2次

## 七、审查关注点

### 高优先级
1. shared-types接口变更影响面(破坏性变更风险)
2. 错误处理完整性(LLM超时/文件权限等边界)
3. 安全漏洞覆盖度(路径遍历/shell注入/symlink攻击)
4. STM compact逻辑正确性(compactedCount vs MAX_COMPACT_COUNT drop模式)

### 中优先级
5. 测试覆盖率达标情况(≥80%), 特别是edge cases
6. ClaudeAdapter/OpenAIAdapter重试逻辑高度重复
7. OpenAIAdapter SSE chunk边界处理鲁棒性

### 低优先级
8. loadConfig() try-catch吞掉所有错误, 调试困难
9. 魔法数字(超时/退避延迟等)集中管理
10. tool-core→llm-adapter间接依赖关系审查
